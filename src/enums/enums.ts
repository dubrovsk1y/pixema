import React from "react";

export enum SidebarTabs {
  Home = "Home",
  Trends = "Trends",
  Favorites = "Favorites",
  Settings = "Settings",
}

export enum Path {
  Home = "/home",
  Trends = "/trends",
  Favorites = "/favorites",
  Settings = "/settings",
}
