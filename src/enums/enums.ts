import React from "react";

export enum SidebarTabs {
  Home = "Home",
  Trends = "Trends",
  Favorites = "Favorites",
  Settings = "Settings",
}

export enum SidebarIcons {
  Home = "home",
  Trends = "local_fire_department",
  Favorites = "bookmark",
  Settings = "settings",
}

export enum Paths {
  Home = "/home",
  Trends = "/trends",
  Favorites = "/favorites",
  Settings = "/settings",
}
